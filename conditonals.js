//  3. Funtion that adds 3 numbers and returns answer

function addNumbers(num1, num2, num3) {
  var sum = num1 + num2 + num3;
  return sum;
}


// 4. console log the answer

var results = addNumbers(5, 8, 10);
console.log(results);